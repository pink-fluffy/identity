import ROLES from './roles';
import ERRORS from './errors';
import { StatusCodes as STATUS_CODES, ReasonPhrases as REASON_PHRASES } from 'http-status-codes';
export default { ROLES, ERRORS, STATUS_CODES, REASON_PHRASES };
